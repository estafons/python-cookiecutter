[lint]
# Enable flake8-bugbear (`B`) rules.
select = ["E", "F", "B", "S", "C90", "N"]

# Ignore `E402` (import violations) in all `__init__.py` files, and in select subdirectories.
[lint.per-file-ignores]
"__init__.py" = ["E402"]
"**/{tests,docs,tools}/*" = ["S"]
